# Copy to .env on the server and fill in. Do not commit the real .env.

# MySQL root password for the bundled database container.
MYSQL_PASSWORD=change-me

# Origin(s) the deployed front end is served from. This is the browser's
# origin, not the API's. Scheme + host + port must match exactly, and a
# trailing slash is not allowed. Comma-separated for more than one.
CORS_ORIGINS=https://invoices.example.com
